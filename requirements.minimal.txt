# Minimal requirements to run the TURN backend (runtime only)
fastapi==0.104.1
uvicorn[standard]==0.24.0
slowapi==0.1.9
aiohttp==3.9.1
sqlalchemy==2.0.23
asyncpg==0.29.0
alembic==1.12.1
pydantic==2.5.0
pydantic-settings==2.0.3
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6
httpx==0.25.2
jinja2==3.1.2
python-dotenv==1.0.0
aiofiles==23.2.0
structlog==23.2.0
# Optional/extra packages (install separately when needed):
# - numpy/scikit-learn
# - torch/transformers/sentence-transformers
# - pillow/weasyprint
# - celery/redis
# - cloudinary
# Install them only if you need the features that depend on them and after
# confirming your Python and platform compatibility.
